# Carlos ERP Web Client — environment variables (example)
# Copy to `.env.local` for local development. Never commit real secrets.

# Base URL of the carlos-backend API (used by the API client in task 45.2).
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api/v1
