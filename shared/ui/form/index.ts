/**
 * Form components barrel (task 45.4).
 *
 * React Hook Form-aware wrappers that bind the design-system primitives to a
 * typed form `control`. Feature forms (task 46.x) compose these with `useForm`
 * + a Zod resolver to get validation and accessible error display for free.
 */
export { FormInput } from './form-input';
export type { FormInputProps } from './form-input';
export { FormSelect } from './form-select';
export type { FormSelectProps } from './form-select';
